{% extends 'AdminBundle::layout.html.twig' %}


    {% block title %}
        Users > {{ "title.user.list" | trans({}, 'FOSUserBundle') }}
    {% endblock%}
    {% block page_title %}
        Users
    {% endblock %}


    {% block page_content %}


        <div class="box">
            <div class="box-header">
                <h3 class="box-title">{{   block('title') }}  </h3>
            <span class="pull-right">
                <a href="{{ path('user_admin_user_new') }}"><button class="btn btn-block btn-success" ">
                    <span class="glyphicon glyphicon-plus-sign" aria-hidden="true"></span> Ajouter un utilisateur</button></a>
            </span>
            </div><!-- /.box-header -->
            <div class="box-body">
                <div id="example1_wrapper" class="dataTables_wrapper form-inline dt-bootstrap">
                    <div class="row">
                        <div class="col-sm-12">
                            <table id="example1" class="table table-bordered table-striped dataTable" role="grid" aria-describedby="example1_info">
                                <thead>
                                <tr>
                                    <th>Id</th>
                                    <th>{{ "form.email" | trans({}, 'FOSUserBundle') }}</th>
                                    <th>{{ "form.actions" | trans({}, 'FOSUserBundle') }}</th>
                                </tr>
                                </thead>
                                <tbody>
                                {% for user in users %}
                                    <tr>
                                        <td>{{ user.id }}</td>
                                        <td>{{ user.email }}</td>
                                        <td>

                                            <a  class="btn btn-info" href="{{ path('user_admin_user_edit', { 'id': user.id }) }}">
                                                <span class="glyphicon glyphicon-edit" aria-hidden="true"></span>
                                                {{ "form.edit" | trans({}, 'FOSUserBundle') }}
                                            </a>

                                        </td>
                                    </tr>
                                {% endfor %}
                                </tbody>
                                <tfoot>
                                <tr>
                                    <th>Id</th>
                                    <th>{{ "form.email" | trans({}, 'FOSUserBundle') }}</th>
                                    <th>{{ "form.actions" | trans({}, 'FOSUserBundle') }}</th>
                                </tr>
                                </tfoot>
                            </table></div>
                    </div>
                </div>

            </div>
            <!-- /.box-body -->
        </div>
    {% endblock %}




    {% block javascripts_inline %}
        <!-- page script -->
        <script type="text/javascript">
            $(function () {
                $('#example1').dataTable();
            });
        </script>

    {% endblock %}

